[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
speck = { path = "../speck" }
multiversion = "0.8"
thiserror = "2"
strum = { version = "0.28", features = ["derive"] }
paste = "1"

[dev-dependencies]
rstest = "0.26"