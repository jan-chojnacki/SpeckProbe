[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
speck = { path = "../speck" }
multiversion = "0.8.0"
thiserror = "2.0.18"

[dev-dependencies]
criterion = "0.8"

[[bench]]
name = "iterator_benches"
harness = false