[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
speck = { path = "../speck" }
thiserror = "2"
strum = { version = "0.28", features = ["derive"] }

[dev-dependencies]
criterion = "0.8"
rstest = "0.26"